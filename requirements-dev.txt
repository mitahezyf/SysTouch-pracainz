-r requirements.txt

# Testy
pytest==8.4.1
pytest-cov==5.0.0

# Formatowanie / lint / typy
black==24.8.0
ruff==0.6.4
mypy==1.11.2
bandit==1.7.9
pre-commit==4.2.0

# Narzedzia pomocnicze
PyYAML==6.0.2
pipreqs==0.4.13
chardet==5.2.0

# GUI (MVP)
PySide6==6.7.2

# ML / PyTorch (CPU)
# Uwaga: instalacja torch na Windows moze wymagac dopasowania do wersji Pythona i CUDA.
# Poniżej bezpieczna wersja CPU-only; w razie potrzeby mozna zainstalowac recznie inny wariant z
# https://pytorch.org/get-started/locally/
torch==2.4.1
pandas>=2.0,<3  # dla datasetu PJM
