﻿# podstawowe biblioteki numeryczne
numpy>=1.24,<3

# obsluga obrazu (wariant headless do CI/serverow)
opencv-python-headless>=4.8,<5
# jesli potrzebne lokalne GUI (imshow), mozna uzywac alternatywnie:
# opencv-python>=4.8,<5  # opcjonalne dla lokalnego GUI

# sledzenie dloni (MediaPipe)
mediapipe>=0.10.14,<0.11

# uczenie maszynowe dla modulu trainer
scikit-learn>=1.3,<2

# PyTorch dla modulu PJM (Sign Language Translator)
# instalacja CPU-only z oficjalnego repo:
# pip install torch==2.4.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
torch==2.4.1; sys_platform == "win32"
pandas>=2.0,<3; sys_platform == "win32"  # dla datasetu PJM
tqdm>=4.66,<5  # pasek postepu dla treningu

# integracje tylko dla Windows (glosnosc, okna, automatyzacja UI)
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.4; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
PyAutoGUI>=0.9.53; sys_platform == "win32"
